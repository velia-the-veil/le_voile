# Consumer rules — vide à ce stade.
# Le module app n'est pas consommé par d'autres modules ; ce fichier est référencé
# par convention AGP. Rules à ajouter si app devient une dépendance d'un autre module.
