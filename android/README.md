# Android — placeholder

Stub directory — Android implementation not yet started.

When implementation begins, mirror the structure of `windows/` or `linux/`:

```
android/
├── cmd/        # entry binaries (client, ui, ctl)
├── internal/   # OS-specific packages (firewall, tun, dns, routing, ui, service, ipc, ...)
├── frontend/   # embedded UI assets (HTML/JS/CSS)
└── README.md
```

Track future Android-specific dependencies (gomobile, Wails Android if/when supported).

Implémentation reportée — voir epic Android (à créer).
